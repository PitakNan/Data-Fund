# CHANGELOG — Data-Fund Portal (portal_v2.html)

## 2026-07-02 — ยกเครื่องใหญ่ทั้งพอร์ทัล (13 commits)

**ผลรวม:** portal_v2.html เล็กลงจาก **2,539KB → 545KB (-78%)** · ทุก dashboard มีมาตรฐานเดียวกัน (narrative + ตัวกรองตรึง + ตารางเรียงได้ + popup drill-down) · หน้า Home ตัวเลขตรงกับหน้าลูกเสมอ · อัปเดตข้อมูลแยกรายไฟล์/ย้อนหลังได้

### 🧠 จิตเวช Homeward (`b56fc49`)
- Trend เปลี่ยนจากรายปีงบ 4 จุด → **รายเดือน 32 จุด** (แท่งเคส + เส้น Readmit% แกนขวา)
- **Readmit 28 วัน ราย PDX / รายกลุ่มโรค** — คอลัมน์ใหม่ sortable พร้อมสี RAG
- คลิกชื่อโรค → popup รายโรงพยาบาลที่รักษา + Readmit% รายแห่ง (เชื่อมสองทางกับ popup รพ.)
- Filter ใหม่: กลุ่มโรค (คลิกชิ้น donut กรองได้) + ระดับหน่วยบริการ A/S/M1/M2/F1–F3
- `_psych_data.json` v2: เพิ่ม field เดือนต่อ case (`extract_psych_v2.py`)
- ⚡ Insight: F122 กัญชา Readmit 34.4%, F112 opioids 24.3%, F152 ยาบ้า 20.2% (2,148 เคส)

### 👶 NB <1,500g (`b56fc49`, `26024d3`)
- เปลี่ยนเป็น **case-level rows** แบบจิตเวช (`extract_nb_v2.py`) → slider ช่วงเวลา**กรองทุกส่วน** (เดิมกรองแค่กราฟ)
- **แก้บั๊ก:** KPI มูลค่าไม่หายเมื่อเลือกปีงบแล้ว
- Narrative box + popup PDX→รพ. เชื่อมสองทาง + popup รพ. เคารพตัวกรองปัจจุบัน
- แก้กราฟเบลอ/ยืดหลังใช้ slider (ตั้ง canvas attribute ก่อนสร้าง chart)
- หมายเหตุ data: ปีงบ 68 ~88% วันที่กระจุก ต.ค.–ธ.ค.67 ตามงวดคืนข้อมูล สปสช. (มี hint เตือนใต้กราฟ)

### 🦵 TKA (`5616f87`, `e1781f9`)
- **แก้ popup กราฟเทรนด์รายเดือนที่พังเงียบมาตั้งแต่ `b04c8fb`** — ตัวแปร `HOSP_MONTHLY` หายตอน re-extract แต่ tooltip ยังอ้างถึง → derive ใหม่จาก `HOSP_MONTH_PDX`
- ตรึงแถบตัวกรองจังหวัดใต้เมนู + **hero "ผลงานจริง" คงภาพรวมเขตเสมอ** ไม่เปลี่ยนตามตัวกรอง (เลือกจังหวัดจะเห็นตัวเลขจังหวัด + % ของเขตที่ KPI ซ้าย)

### 🔁 ส่งต่อออกนอก สป.สธ. — ยกเครื่องทั้งหน้า (`98d3d8f`)
- ตัด `RM` blob 914KB ฝังในไฟล์ → fetch `_refer_data.json` (case-level 43,694 แถว จาก Excel สปสช. คืนข้อมูล 29 พ.ค.69, จังหวัดต้นทาง join จาก health_units_rh1_master ครบ 100%, ตัวเลขตรง RM เดิมทุกตัว)
- ใหม่: **trend รายเดือน 44 เดือน**, narrative, ตารางปลายทาง 21 แห่ง + ตาราง PDX 2,801 รหัส sortable, popup ปลายทาง↔PDX เชื่อมสองทาง, ตัวกรองตรึง
- คงไว้: Matrix ต้นทาง×ปลายทาง + drill รายโรค, กราฟจังหวัด×ปีงบ + เส้นเทรนด์
- ⚡ Insight: รพ.มหาราช มช. รับ 58.7% ของการส่งออก · โรคส่งออกอันดับ 1 = F2000 จิตเภท → รพ.สวนปรุง

### 💊 PPFS (`1c6b9e0`, `7bacc64`)
- เปลี่ยนชื่อเมนู "PP กองทุน" → **PPFS** + narrative + ตารางหน่วยบริการ sortable + ตัวกรองตรึง + แก้กราฟเบลอ
- **Redesign:** ถอดกราฟแท่ง 26 กองทุน×3ปี (820px, สเกลเดียว อ่านไม่ได้) แทนด้วย
  - **ตารางจังหวะการเบิกจ่าย** — bullet bar เทียบทั้งปี 68 + ขีด "ควรเบิกได้ ณ วันที่ข้อมูล" (คำนวณอัตโนมัติจาก `meta.as_of`) + Pace badge เขียว≥90% เหลือง 60–90% แดง<60%
  - **Heatmap จังหวัด × Top 12 กองทุน** — สี = pace รายจังหวัด คลิกช่องเจาะกองทุน+จังหวัด
- ⚡ Insight: วัคซีน (25.) pace แดงเกือบทุกจังหวัด (เขต 54%) = ปัญหาเชิงระบบ · แว่นตาเด็ก 3% · พาร์กินสัน 29% · ดาวน์ 12% — เงินเสี่ยงหลุดปลายปี

### 🏠 หน้า Home v2 (`66e66c4`)
- การ์ด 6 ใบ (เพิ่ม PPFS) คำนวณจาก **`_home_cards.json`** ที่สร้างโดย `build_home_cards.py` จาก JSON จริง 5 ประเด็น + TKA จากค่าในหน้า → **ตัวเลข Home ตรงกับหน้าลูกเสมอ**
- แต่ละการ์ดแสดง **วันที่ข้อมูลของตัวเอง** — รองรับการอัปเดตไม่พร้อมกัน
- ลบ blob เก่าทั้งหมด (DB/INS/ME) + หน้า "ภาพรวม" ที่ซ่อนไว้ + dead code

### 🧹 โครงสร้าง (`645fd00`, `65d8485`, `1239182`)
- ตัด `FD` blob 570KB (progress bar คำนวณจากข้อมูลจริงของแต่ละหน้าแทน — ค่า adjRW เปลี่ยน 49,197.6 → 43,974.8 ตามแหล่ง AdjRW 0.5 LOS (2).xlsx ที่เป็นมาตรฐาน)
- ลบ dead code 225 บรรทัด (renderIssue/M&E renderers เก่า + trap `renderNBme`)
- `#tka` deep link ใช้งานได้ · ตรึง filter card NB/จิตเวช
- **fetch ทุก dashboard เป็น `cache:'no-cache'`** — push ไฟล์ JSON ตัวเดียวเห็นผลทันที ไม่ต้อง Ctrl+F5

---

## 📋 วิธีอัปเดตข้อมูล (3 ขั้นตอน)

เมื่อได้ Excel รอบใหม่ของประเด็นใดประเด็นหนึ่ง:

1. **รัน extractor ของประเด็นนั้น** → ได้ `_xxx_data.json` ใหม่ (rebuild ทั้งไฟล์ — การแก้ยอดย้อนหลังใน Excel ถูกแทนอัตโนมัติทุกปี/เดือน)
2. **รัน `python build_home_cards.py`** (ในโฟลเดอร์นี้ อ่าน JSON local ~1 วินาที) → `_home_cards.json` เพื่อให้การ์ดหน้า Home ตรงกับหน้าลูก
3. **Commit + push** เฉพาะ JSON ที่เปลี่ยน + `_home_cards.json`

> เงื่อนไข: Excel รอบใหม่ต้องมีโครงสร้างคอลัมน์เหมือนเดิม · อัปเดตแยกรายประเด็น/ไม่พร้อมกันได้ การ์ด Home แสดงวันที่ข้อมูลรายใบ

| ประเด็น | ไฟล์ข้อมูล | หมายเหตุ |
|---|---|---|
| NB <1,500g | `_nb_data.json` | case-level 7,295 แถว |
| จิตเวช | `_psych_data.json` | case-level 7,799 แถว + readmit flag |
| ส่งต่อออกนอก | `_refer_data.json` | case-level 43,694 แถว |
| adjRW<0.5 | `_adjrw_los2.json` (+2 raw) | pre-aggregated รายเดือน |
| PPFS | `_ppfs_data.json` | svc_yr + รายหน่วยบริการ |
| Home cards | `_home_cards.json` | สร้างโดย `build_home_cards.py` |
| TKA | ฝังใน portal_v2.html | ยังไม่แยกไฟล์ (งานอนาคต) |
